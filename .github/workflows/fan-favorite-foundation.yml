name: Fan-Favorite Foundation

on:
  pull_request:
  push:
    branches:
      - main
      - polish/fan-favorite-foundation

permissions:
  contents: read

concurrency:
  group: creator-studio-foundation-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Secrets, lint, typecheck, enterprise tests, build, and smoke
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install locked dependencies
        id: install
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          npm ci 2>&1 | tee install.log

      - name: Scan tracked files for secrets
        id: secrets
        if: steps.install.outcome == 'success'
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          npm run secrets:check 2>&1 | tee secrets.log

      - name: Lint
        id: lint
        if: steps.install.outcome == 'success'
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          npm run lint 2>&1 | tee lint.log

      - name: Typecheck
        id: typecheck
        if: steps.install.outcome == 'success'
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          npm run typecheck 2>&1 | tee typecheck.log

      - name: Validate development environment policy
        id: envcheck
        if: steps.install.outcome == 'success'
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          npm run env:check 2>&1 | tee envcheck.log

      - name: Run focused enterprise foundation tests
        id: enterprise
        if: steps.install.outcome == 'success'
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          npm run test:enterprise 2>&1 | tee enterprise.log

      - name: Build
        id: build
        if: steps.install.outcome == 'success'
        continue-on-error: true
        shell: bash
        env:
          NEXT_TELEMETRY_DISABLED: "1"
        run: |
          set -o pipefail
          npm run build 2>&1 | tee build.log

      - name: Smoke-test repository contract
        id: smoke
        if: steps.install.outcome == 'success'
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          npm run test:smoke 2>&1 | tee smoke.log

      - name: Record outcomes
        if: always()
        shell: bash
        run: |
          printf '%s\n' \
            "install_outcome=${{ steps.install.outcome }}" \
            "secrets_outcome=${{ steps.secrets.outcome }}" \
            "lint_outcome=${{ steps.lint.outcome }}" \
            "typecheck_outcome=${{ steps.typecheck.outcome }}" \
            "envcheck_outcome=${{ steps.envcheck.outcome }}" \
            "enterprise_outcome=${{ steps.enterprise.outcome }}" \
            "build_outcome=${{ steps.build.outcome }}" \
            "smoke_outcome=${{ steps.smoke.outcome }}" \
            > foundation-outcomes.txt

      - name: Upload foundation evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phoenix-creator-studio-foundation
          path: |
            install.log
            secrets.log
            lint.log
            typecheck.log
            envcheck.log
            enterprise.log
            build.log
            smoke.log
            foundation-outcomes.txt
            .next/build-manifest.json
            .next/routes-manifest.json
          if-no-files-found: warn
          retention-days: 14

      - name: Enforce foundation gate
        if: always()
        shell: bash
        run: |
          test "${{ steps.install.outcome }}" = "success"
          test "${{ steps.secrets.outcome }}" = "success"
          test "${{ steps.lint.outcome }}" = "success"
          test "${{ steps.typecheck.outcome }}" = "success"
          test "${{ steps.envcheck.outcome }}" = "success"
          test "${{ steps.enterprise.outcome }}" = "success"
          test "${{ steps.build.outcome }}" = "success"
          test "${{ steps.smoke.outcome }}" = "success"
